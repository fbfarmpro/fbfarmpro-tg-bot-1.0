version: "3.8"

services:
  fbfarm:
    build: .
    stop_signal: SIGINT
    restart: on-failure
    volumes:
      - ./DB:/app/DB
    ports:
      - "5000:5000"
    depends_on:
      - redis
    networks:
      - default

  redis:
    image: "redis:alpine"
    ports:
      - "6379"
    networks:
      - default


networks:
  default:
    driver: bridge